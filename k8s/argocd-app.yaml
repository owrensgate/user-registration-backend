apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-registration-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/owrensgate/user-registration-backend.git
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true